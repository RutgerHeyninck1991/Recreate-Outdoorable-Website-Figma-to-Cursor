import React, { useState } from 'react';import { Link } from 'react-router-dom';import { Button } from './ui/button';import { Input } from './ui/input';import { Label } from './ui/label';import { Card, CardContent, CardDescription, CardHeader, CardTitle } from './ui/card';import outdoorableLogo from '../assets/23cc91becaaf56a7cc57ff9c082be886de142ac2.png';export default function Login() {  const [isLogin, setIsLogin] = useState(true);    return (    <div className="min-h-screen flex items-center justify-center bg-stone-50 px-4">      <div className="w-full max-w-md">        {/* Header */}        <div className="text-center mb-8">          <Link to="/" className="hover:opacity-80 transition-opacity inline-block mb-2">            <img src={outdoorableLogo} alt="Outdoorable" className="h-24 mx-auto" />          </Link>          <p className="text-stone-600 mt-2">Welkom bij je outdoor kussen configurator</p>        </div>        <Card className="shadow-lg border-stone-200">          <CardHeader className="text-center">            <CardTitle className="text-2xl font-light text-stone-800">              {isLogin ? 'Inloggen' : 'Account aanmaken'}            </CardTitle>            <CardDescription className="text-stone-600">              {isLogin                 ? 'Log in om je opgeslagen ontwerpen te bekijken'                 : 'Maak een account aan om je ontwerpen op te slaan'              }            </CardDescription>          </CardHeader>          <CardContent className="space-y-4">            <div className="space-y-2">              <Label htmlFor="email" className="text-stone-700">E-mailadres</Label>              <Input                id="email"                type="email"                placeholder="naam@voorbeeld.nl"                className="border-stone-300 focus:border-stone-500"              />            </div>                        <div className="space-y-2">              <Label htmlFor="password" className="text-stone-700">Wachtwoord</Label>              <Input                id="password"                type="password"                placeholder="••••••••"                className="border-stone-300 focus:border-stone-500"              />            </div>            {!isLogin && (              <div className="space-y-2">                <Label htmlFor="confirmPassword" className="text-stone-700">Bevestig wachtwoord</Label>                <Input                  id="confirmPassword"                  type="password"                  placeholder="••••••••"                  className="border-stone-300 focus:border-stone-500"                />              </div>            )}            <Button               className="w-full bg-stone-800 hover:bg-stone-900 text-white py-6 rounded-xl text-lg"            >              {isLogin ? 'Inloggen' : 'Account aanmaken'}            </Button>            <div className="text-center">              <button                type="button"                onClick={() => setIsLogin(!isLogin)}                className="text-stone-600 hover:text-stone-800 text-sm underline"              >                {isLogin                   ? 'Nog geen account? Maak er een aan'                   : 'Al een account? Log hier in'                }              </button>            </div>            <div className="text-center pt-4 border-t border-stone-200">              <Link                 to="/"                className="text-stone-600 hover:text-stone-800 text-sm"              >                ← Terug naar homepagina              </Link>            </div>          </CardContent>        </Card>      </div>    </div>  );}
